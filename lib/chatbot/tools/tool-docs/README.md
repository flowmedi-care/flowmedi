# Tool Documentation

Markdown files in this directory are the **source of truth** for each tool capability.

Compact OpenAI `description` fields are in [`descriptions.ts`](./descriptions.ts) — sync when updating docs.

Template: Purpose, Inputs, Output, When to use, When NOT, Failure modes, Examples.
